<configuration>

    <!-- Other configurations for appenders, loggers, etc. -->

    <!-- Configuration for logging Hibernate SQL with parameter values -->
    <logger name="org.hibernate.type.descriptor.sql" level="TRACE"/>

    <!-- Root logger -->
    <root level="INFO">
        <appender-ref ref="STDOUT"/>
    </root>
</configuration>